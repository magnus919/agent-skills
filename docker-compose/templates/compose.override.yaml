services:
  api:
    build: .
    profiles: [app]
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
          initial_sync: true
          ignore: [node_modules/]
        - action: rebuild
          path: package.json
